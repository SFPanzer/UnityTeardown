# Unity Teardown
The project is a voxel-based physics destruction game similar to Teardown, developed using Unity. 

It utilizes scriptable rendering pipelines and compute shaders to achieve the desired visual effects.